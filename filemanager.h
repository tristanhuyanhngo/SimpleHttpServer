#ifndef FILEMANAGER_H
#define FILEMANAGER_H

#include <string>
#include <iostream>
#include <QDir>
#include <QDateTime>

using namespace std;

class FileManager {
public:
    FileManager(string location);
};

#endif // FILEMANAGER_H
